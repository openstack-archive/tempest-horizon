============
Installation
============

At the command line::

    $ pip install tempest-horizon

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv tempest-horizon
    $ pip install tempest-horizon
